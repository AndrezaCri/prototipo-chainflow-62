# Configuração para deploy em redes reais
# NUNCA commitar a .env real - apenas este arquivo .env.example

# Chave privada para deploy (sem 0x no início)
PRIVATE_KEY=sua_chave_privada_aqui

# API Key do Basescan (opcional, para verificação de contratos)
BASESCAN_API_KEY=sua_chave_api_aqui